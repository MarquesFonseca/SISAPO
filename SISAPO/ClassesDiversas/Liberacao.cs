using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SISAPO.ClassesDiversas
{
    public static class Liberacao
    {
        /// <summary>
        /// 
        /// </summary>
        /// <param name="QuantidadeMaxima">Informe a quantidade máxima de execução do sistema</param>
        /// <param name="Chave">Informe a chave do registro do windows</param>
        public static void LiberacaoDemo(int QuantidadeMaxima, string Chave)
        {
            
        }

        public static int ChaveLiberacaoDemo(string CodigoBase)
        {
            return 0;
            //string _versao = Versao.RetornaVersao();
            //return Convert.ToInt32(CodigoBase) * 209 + Convert.ToInt32(DateTime.Now.Year) - Convert.ToInt32(DateTime.Now.Day) / Convert.ToInt32(DateTime.Now.Month) / Convert.ToInt32(DateTime.Now.Year) - Convert.ToInt32(DateTime.Now.Month);
        }
    }
}
